///draw_self();
///This function draws the sprite assigned to the instance exactly as it would be drawn if the draw event held no code or actions,
///and will reflect and changes that have been made to the sprite variables in other events.

return draw_sprite_ext(sprite_index,-1,x,y,image_xscale,image_yscale,image_angle,image_blend,image_alpha);
